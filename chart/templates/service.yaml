apiVersion: v1
kind: Service
metadata:
  name: {{ include "eth-view.fullname" . }}
  labels:
    {{- include "eth-view.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eth-view.selectorLabels" . | nindent 4 }}
